import React from 'react'
import { styled } from 'styled-components'

const Section = styled.div``
const ImageUrl = styled.div``

const Carousel = (props) => {
  return <Section>
    <ImageBanner imageArray={Float32Array.picture}
  </Section>
}
export default Carousel
